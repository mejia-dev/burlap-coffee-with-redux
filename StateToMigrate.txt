
Shared State:

 - dataBagCurrentlySelected goes to BagDetails as "currentBag"
 - dataBagCurrentlySelected goes to BagChangeForm as "currentBag" (as an option for "passedBag" variable)
  - these should be simple. Prop is already an object in both locations.

 - dataBagInventory goes to InventoryList as "coffeeBagInventory"
  - may need an Object.values to map items, will investigate

 - displayBagChangeMode goes to BagChangeForm as "changeMode"
  - Purely graphical -- can be removed if running close to deadline